package com.spiritwisestudios.todolist.ui.theme

import androidx.compose.ui.graphics.Color

// This file defines the color palette used in the Jetpack Compose theme.
// These colors are referenced in Theme.kt file to create consistent light and dark color schemes.

// Darker variant colors (typically used for backgrounds, surfaces, or accents in dark themes)
val Purple80 = Color(0xFFD0BCFF)        // Light purple used as a primary elements in dark mode
val PurpleGrey80 = Color(0xFFCCC2DC)    // Muted purple used for secondary elements in dark mode
val Pink80 = Color(0xFFEFB8C8)          // Soft pink used for highlights in dark mode

// Lighter variant colors (typically used for primary elements in light themes)
val Purple40 = Color(0xFF6650a4)        // A deep purple used as primary color in light mode
val PurpleGrey40 = Color(0xFF625b71)    // A darker purple used for secondary elements in light mode
val Pink40 = Color(0xFF7D5260)          // Darker pink, used for accents in light mode